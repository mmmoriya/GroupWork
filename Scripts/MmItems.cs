using System.Collections;
using System.Collections.Generic;
using Unity.Collections.LowLevel.Unsafe;
using UnityEngine;

public class MmItems : MonoBehaviour
{
    //public GameObject[] itemPrefabs; //生成されるItem
    //public GameObject Items;

    //public Transform itemParentTransform;//HierarchyでCandiesグループがどれかを指定

    public int number;

    //public MmGameController itemCon;

    // Start is called before the first frame update
    void Start()
    {
        //int random = Random.Range(0,itemPrefabs.Length);
        //Instantiate(Items, itemPrefabs[random]);
    }

    // Update is called once per frame
    void Update()
    {

    }
    
}
